# Natural-language search (header search box). Server-side only - never exposed
# to the browser. Without it, search silently falls back to plain text search.
# Local: copy to .env.local. Vercel: Project Settings -> Environment Variables.
ANTHROPIC_API_KEY=
